<Project>
    <PropertyGroup>
        <!-- Replace MyPC build path -->
        <RepoRoot>$([System.IO.Path]::GetFullPath('$(MSBuildThisFileDirectory)..\'))</RepoRoot>
        <PathMap>$(RepoRoot)=.</PathMap>
    </PropertyGroup>
</Project>
